# Frontend

Minimal Next.js UI for the Ask about Piotr chat interface.

## Run locally
```bash
npm install
npm run dev
```

By default, the UI calls `http://localhost:8000/chat`. Override with
`NEXT_PUBLIC_API_URL` if needed.


